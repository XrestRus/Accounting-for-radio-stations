{% extends 'base.html.twig' %}

{% block title %}Учет радиостанций - Ошибка доступа{% endblock %}

{% block body %}
<div class="access-denied-container">
    <div class="card text-center">
        <div class="card-header bg-danger text-white">
            <h2><i class="bi bi-x-octagon"></i> Ошибка доступа</h2>
        </div>
        <div class="card-body">
            <h5 class="card-title mb-4">Доступ запрещен</h5>
            <p class="card-text">
                К сожалению, у вас нет прав доступа к запрошенной странице или функции.
            </p>
            <p class="card-text mb-4">
                Если вы считаете, что это ошибка, пожалуйста, обратитесь к администратору системы.
            </p>
            <a href="{{ path('app_dashboard') }}" class="btn btn-primary">
                <i class="bi bi-house"></i> Вернуться на главную
            </a>
        </div>
        <div class="card-footer text-muted">
            Роль пользователя: {{ app.user ? app.user.roles|join(', ')|replace({'ROLE_': '', '_': ' '})|lower|capitalize : 'Гость' }}
        </div>
    </div>
</div>
{% endblock %}

{% block stylesheets %}
{{ parent() }}
<style>
    .access-denied-container {
        max-width: 600px;
        margin: 5rem auto;
    }
</style>
{% endblock %} 